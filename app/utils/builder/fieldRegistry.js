/**
 * Field Registry — app wrapper
 *
 * Canonical implementation lives in shared/builder/fieldRegistry.js so both
 * Nuxt app code and Nitro server code import the same runtime-safe source.
 */
export * from '#shared/builder/fieldRegistry.js'
